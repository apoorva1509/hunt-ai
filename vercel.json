{
  "installCommand": "npm install && cd web && npm install",
  "buildCommand": "cd web && rm -f convex && cp -r ../convex . && npm run build",
  "outputDirectory": "web/.next",
  "framework": "nextjs"
}
